/**
 * Compare two VDOM trees and return a patch list.
 * Team member B will replace this starter implementation.
 */
export function diff(oldVDOM, newVDOM, path = []) {
  void oldVDOM;
  void newVDOM;
  void path;

  return [];
}

